import 'package:flutter/material.dart';

void main()=>runApp(
  new CurvedAnimate()
);
class CurvedAnimate extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }

}